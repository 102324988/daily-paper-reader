---
title: "HGSQ: Heatmap-Guided Sparse Query Detector for Real-Time Aerial Small Object Detection"
title_zh: HGSQ：面向实时航拍小目标检测的热图引导稀疏查询检测器
authors: Yangchen Zeng
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.13306v1"
tags: ["query:cv"]
score: 9.0
evidence: 面向实时小目标检测的稀疏查询检测器
tldr: 航拍小目标实时检测需精细定位并减少背景冗余计算，现有Transformer检测器因密集查询和多层解码，在稀疏目标区域浪费算力。HGSQ用轻量热图预算预测器单次前向生成前景预算图，据此进行稀疏查询选择、热图门控Lite Snake卷积局部细化与自适应查询-解码预算。在NWPU VHR-10和VisDrone2019上分别达95.10与54.8 mAP50，GFLOPs降至48.6，RTX 4070 TensorRT FP16下96 FPS。该方法把热图作为实时计算预算而非可视化图，实现高效航拍小目标检测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.13306v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 791, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.13306v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1421, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.13306v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 789, \"height\": 439, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.13306v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1664, \"height\": 510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 731, \"height\": 269, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 699, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1663, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 715, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.13306v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 551, \"height\": 200, \"label\": \"Table\"}]"
motivation: 现有Transformer检测器全局建模强，但密集查询初始化和多层解码器在背景token上开销大，小目标仅占稀疏区域时效率低。
method: HGSQ以轻量HBP单次预测前景预算热图，并固定采用HSQS稀疏查询选择、HGLSConv局部形状细化和AQDB自适应查询-解码预算。
result: 在NWPU VHR-10上达到95.10 mAP50，在VisDrone2019上达到54.8 mAP50；计算量降至48.6 GFLOPs，实时性显著提升，并在RTX 4070的TensorRT FP16部署下达到96.0 FPS。
conclusion: 将热图视为实时计算预算而非事后可视化图，面向航拍/UAV部署实现高效小目标检测，而非通用检测器。
---

## 摘要
实时航拍小目标检测是一个重要的视觉信号与图像处理问题，要求检测器在保持细粒度定位的同时，避免在大型背景区域上进行冗余计算。本文聚焦于这种面向部署的航拍/无人机场景，而非声称可适用于所有目标检测场景的通用检测器。现有的基于Transformer的检测器具有强大的全局建模能力，但其密集查询初始化和多层解码器仍会在背景token上耗费大量计算，当小目标仅占据稀疏图像区域时，这种计算并不高效。为解决该问题，本文提出HGSQ，一种用于实时航拍小目标检测的热图引导稀疏查询检测器。HGSQ使用轻量级热图预算预测器（HBP）在单次前向传播中预测前景预算图。随后，预测得到的热图被三个固定组件使用：热图引导稀疏查询选择（HSQS），其从高置信度前景位置初始化解码器查询；热图门控轻量蛇形卷积（HGLSConv），其仅在热图激活的小目标区域上执行局部形状细化；以及自适应查询-解码器预算（AQDB），其根据估计的目标密度调整查询预算和解码器深度。与事后生成热图不同，HGSQ在部署时将热图视为实时计算预算，而非可视化图。在NWPU VHR-10和VisDrone2019上的实验表明，HGSQ在NWPU VHR-10上达到95.10 mAP50，在VisDrone2019上达到54.8 mAP50，同时将GFLOPs降至48.6，并在我们的TensorRT FP16部署协议下于RTX 4070上以96.0 FPS运行。

## Abstract
Real-time aerial small object detection is an important visual signal and image processing problem, requiring a detector to preserve fine-grained localization while avoiding redundant computation on large background regions. This paper focuses on this deployment-oriented aerial/UAV setting rather than claiming a universal detector for all object detection scenarios. Existing Transformer-based detectors provide strong global modeling, but their dense query initialization and multi-layer decoder still spend substantial computation on background tokens, which is inefficient when small objects occupy only sparse image regions. To address this problem, this paper proposes HGSQ, a Heatmap-Guided Sparse Query Detector for real-time aerial small object detection. HGSQ uses a lightweight Heatmap Budget Predictor (HBP) to predict a foreground budget map in a single forward pass. The predicted heatmap is then used by three fixed components: Heatmap-Guided Sparse Query Selection (HSQS), which initializes decoder queries from high-confidence foreground positions; Heatmap-Gated Lite Snake Convolution (HGLSConv), which performs local shape refinement only on heatmap-activated small-object regions; and Adaptive Query-Decoder Budgeting (AQDB), which adjusts the query budget and decoder depth according to the estimated object density. Unlike post-hoc heatmap generation, HGSQ treats the heatmap as a real-time computation budget rather than a visualization map during deployment. Experiments on NWPU VHR-10 and VisDrone2019 show that HGSQ achieves 95.10 mAP50 on NWPU VHR-10 and 54.8 mAP50 on VisDrone2019, while reducing GFLOPs to 48.6 and running at 96.0 FPS on an RTX 4070 under our TensorRT FP16 deployment protocol.

---

## 论文详细总结（自动生成）

# HGSQ 论文中文总结

## 1. 核心问题与研究动机

- **研究背景**：实时航拍小目标检测是遥感与 UAV 感知中的重要任务，属于典型的实时图像/信号处理问题。检测器需要在高背景占比图像中定位密集、微小目标，同时满足严格延迟与吞吐约束。
- **现有方法矛盾**：
  - CNN 类实时检测器（如 YOLO 系列）效率高，但局部特征聚合对复杂航拍场景中的密集小目标可能不足。
  - Transformer 类检测器（DETR、RT-DETR 等）全局建模强，但采用密集查询初始化和多层解码器，会在大量背景 token 上浪费计算。
- **核心问题**：航拍图像中仅少量空间位置包含小目标，而密集 Transformer 解码仍对前景与背景分配相近计算量，这种“计算分配错配”限制了精度-速度权衡。
- **论文定位**：HGSQ 聚焦面向部署的航拍/UAV 小目标检测，不宣称是通用目标检测器，也不是通用可视化方法。其核心思想是把轻量热图作为**实时计算预算图**，而非事后可视化或 Grad-CAM 解释图。

## 2. 方法论

### 2.1 核心思想

- 使用轻量级 **Heatmap Budget Predictor（HBP）** 在单次前向传播中预测前景预算热图 \(H_b \in [0,1]^{H\times W}\)。
- 热图在部署时直接决定三件事：
  1. 哪些位置成为解码器查询；
  2. 哪些区域进行局部形状细化；
  3. 每个图像分配多少查询预算与解码深度。
- 全流程单次前向、确定性，不使用反向传播、Grad-CAM、高阶导数或第二次前向。

### 2.2 关键技术细节

- **HBP（热图预算预测器）**：
  - 结构：3×3 卷积 + 归一化与激活 + 1×1 投影 + Sigmoid 输出。
  - 训练目标：由 GT 框中心放置高斯响应生成 \(H_{gt}\)，尺度与目标投影大小成比例。
  - 监督：focal heatmap loss，与检测器联合训练，不使用离线教师热图。
  - 推理时仅为普通前向分支，不引入额外推理 pass。

- **HSQS（热图引导稀疏查询选择）**：
  - 给定查询预算 \(K\)，在空间索引集上取热图得分最高的 Top-K 位置 \(P_K\)。
  - 初始查询由对应位置的特征向量、位置编码以及热图分数嵌入线性投影组合而成：
    \[
    q_m^0 = W_q F(p_m) + W_p PE(p_m) + W_h e(H_b(p_m))
    \]
  - 查询矩阵 \(Q^0 \in \mathbb{R}^{K\times d}\) 仅依赖当前前向的特征与热图，具有因果性。
  - 热图还可调制位置编码：\(PE'(i,j)=PE(i,j)\odot(1+\gamma H_b(i,j))\)，增强前景同时保留背景上下文，比二值掩码更稳定。

- **HGLSConv（热图门控轻量蛇形卷积）**：
  - 动机：小目标仅占少数特征单元，标准卷积采样固定；可变形卷积虽灵活，但密集偏移预测与密集采样成本高。
  - 设计：
    - 轴向（水平/垂直）轻量蛇形采样，而非全二维采样；
    - 偏移由轻量预测器生成，并用 tanh 有界约束，防止无控制形变；
    - 非整数坐标用双线性插值；
    - 热图缩放到特征分辨率得到门控 \(G\)，仅在 \(G(i,j)>\tau_g\) 的激活位置 \(A\) 上执行蛇形采样与轴向细化，再 scatter 回原布局。
  - 输出：\(F_{out}=F+G\odot S_A(F)\)，背景保持原始轻量特征，高预算区域获得局部细化。
  - 主要采样成本随激活位置数 \(|A|\) 缩放，而非全图 \(HW\)。

- **AQDB（自适应查询-解码器预算）**：
  - 从热图估计图像级目标密度：
    \[
    \rho=\frac{1}{HW}\sum
