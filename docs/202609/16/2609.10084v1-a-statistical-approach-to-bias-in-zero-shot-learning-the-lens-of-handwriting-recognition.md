---
title: "A statistical approach to bias in zero-shot learning: the lens of handwriting recognition"
title_zh: 零样本学习中偏差的统计方法：手写识别的视角
authors: "Clarence Chew, Gim Siang Chia, Sukalpa Chanda, Subhroshekhar Ghosh, Soumendu Sundar Mukherjee"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.10084v1"
tags: ["query:cv"]
score: 8.0
evidence: 面向视觉识别系统的广义零样本学习
tldr: "广义零样本学习（GZSL）需泛化到未见类，但传统方法因对已见类的误分类偏差，难以扩展至大规模未见类。本文以超大词汇零样本手写词识别为场景，提出统计偏差校正法：将经典GZSL学习器视为黑箱，采用两阶段架构，先预测训练状态，再用轻量蒙特卡洛校正器集成纠偏。校正后仅在预测状态内用最近邻、逻辑回归和随机森林分类，未见词相对准确率提升超20%，大规模词识别可降至约15维表示，并有数学分析支撑。该方法可即插即用结合任意经典GZSL学习器。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.10084v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1555, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.10084v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1100, \"height\": 439, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.10084v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1318, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.10084v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1678, \"height\": 431, \"label\": \"Table\"}]"
motivation: 传统GZSL对已见类存在误分类偏差，难以处理大规模未见类，超大词汇手写词识别更凸显该瓶颈。
method: 将经典GZSL学习器作黑箱，采用两阶段架构：先预测样本训练状态，再用轻量蒙特卡洛校正器集成纠偏，并在预测状态内分类。
result: "未见词分类相对准确率较已有方法提升超20%；大规模词识别可压缩至约15维表示，并获数学分析支持。"
conclusion: 该方法可即插即用地结合任意经典GZSL学习器，为不同领域GZSL的偏差校正提供通用方案。
---

## 摘要
广义零样本学习（GZSL）已成为视觉识别系统的一种重要范式，这类系统必须泛化到训练期间未观察到的类别。传统 GZSL 技术受限于其仅适用于相对少量此类未见类别；由于其对训练期间已观察类别存在众所周知的误分类偏差，超出这一范围的可扩展性颇具挑战。在这项工作中，我们通过超大规模词汇表上的零样本手写单词识别来考察 GZSL 范式。我们提出一种统计方法来矫正这一偏差，该方法将任何经典 GZSL 特征学习器视为黑箱机制，其在对典型数据点的训练状态（已见 vs. 未见）进行识别时具有内在偏差，而我们的目标就是矫正该偏差，这类似于一个分布外推断问题。我们的方法利用一个简单的两阶段层次化架构，在第一阶段结合经典 GZSL 黑箱，在第二阶段结合一组轻量级蒙特卡洛偏差校正器。一旦完成去偏，测试数据的分类仅在其预测的训练状态范围内进行，并采用有充分依据的统计方法（例如最近邻、逻辑回归和随机森林）。与已有技术相比，我们在未见单词分类上实现了超过 20% 的相对准确率提升。一个关键结果是，大规模词汇表上的单词识别可以采用维度低得多的表示（约 15 维）。我们的方法以数学分析为支撑，该分析捕捉了偏差校正统计方法的本质。我们的偏差矫正方法可以以即插即用的方式与任何作为黑箱的经典 GZSL 学习器结合，从而表明该方法对不同领域中各种 GZSL 实现具有广泛的适用性。

## Abstract
Generalized zero-shot learning (GZSL) has emerged as an important paradigm for visual recognition systems that must generalize to classes that were not observed during training. Traditional GZSL techniques are limited by their applicability to a relatively small number of such unseen classes, scalability beyond which is challenging due to its well-known misclassification bias towards classes observed during training. In this work, we investigate the GZSL paradigm through the lens of zero-shot handwritten word recognition over extremely large vocabularies. We propose a statistical approach to rectifying this bias, which views any classical GZSL feature learner as a black box mechanism whose intrinsic bias in identifying the training status (seen vs. unseen) of a typical data point we aim to correct, similar to an out of distribution inferential problem. Our method leverages a simple two-stage hierarchical architecture, combining a classical GZSL blackbox in the first stage and an ensemble of lightweight Monte Carlo bias-correctors in the second. Once debiased, the classification of test data is undertaken only restricted to its predicted training status via well-founded statistical methods (eg nearest neighbour, logistic regression and random forests). We achieve relative accuracy improvements of over 20% in the classification of unseen words compared to established techniques. A key outcome is that word recognition over large scale vocabularies is amenable to a much lower dimensional representation (~15 dimensions). Our approach is underpinned by mathematical analysis that captures the essence of the statistical approach to bias correction. Our approach to bias rectification can be combined in a turn-key fashion with any classical GZSL learner as a blackbox, thereby suggesting a wide scope of applicability of this method for a wide variety of GZSL implementations in different domains.

---

## 论文详细总结（自动生成）

# 论文总结：零样本学习中偏差的统计方法——手写识别视角

## 1. 核心问题与整体含义

- **研究动机**：广义零样本学习（GZSL）要求模型同时处理训练中已见类与测试中未见类，但传统 GZSL 方法存在明显的“已见类偏差”，即倾向把测试样本误判为训练阶段见过的类，导致未见类识别性能显著下降。
- **场景选择**：论文以“超大规模词汇表上的零样本手写单词识别”为切入点。相比 AwA、CUB、SUN 等对象识别 benchmark，手写词识别的类别空间是组合式的，词类数量可达数千，且词与词之间可能只差一个字符或笔画结构，类间差异细微、类内差异大。
- **核心矛盾**：现有 Pho(SC)Net 等结构化词嵌入方法虽能支持零样本词识别，但仍表现出强已见类偏差：在 IAM 数据上，已见类准确率约 0.93，未见类准确率仅约 0.77。
- **整体含义**：论文将 GZSL 偏差校正视为一个统计推断问题：判断一个测试样本的类别是否来自训练时见过的类，即“训练状态识别/分布外推断”问题。目标是在不改变经典 GZSL 特征学习器黑箱的前提下，对其输出进行去偏，从而提升未见类识别能力。

## 2. 方法论

### 核心思想

- 将任意经典 GZSL 特征学习器视为黑箱，不直接修改其特征学习过程。
- 采用两阶段层次化架构：
  - **第一阶段**：使用多个结构化词嵌入模型 \(M^{(k)}_{[1]}\) 生成词图像的特征表示。
  - **第二阶段**：为每个第一阶段模型训练一组轻量级“已见/未见过”分类器 \(M^{(k),[2]}_i\)，通过蒙特卡洛式集成估计样本的训练状态，并进行阈值校准，以显式矫正已见类偏差。
- 去偏后，只在预测出的训练状态对应词表内进行分类：若预测为已见，则在已见词表 \(W_s^{(k)}\) 内匹配；若预测为未见，则在剩余词表 \(W \setminus W_s^{(k)}\) 内匹配。

### 关键技术细节

- **第一阶段：嵌入模型集合**
  - 训练 \(L\) 个嵌入模型 \(M^{(k)}_{[1]}\)。
  - 每个模型只在部分已见词类 \(W_s^{(k)} \subset W_s\) 上训练，其余词类对其视为未见。
  - 这样得到一组具有部分知识差异的嵌入模型，为后续集成提供多样性。
- **第二阶段：已见/未见分类器集成**
  - 对每个 \(M^{(k)}_{[1]}\)，训练 \(n_k\) 个二值分类器 \(M^{(k),[2]}_i\)，判断嵌入特征是否来自该模型训练时见过的类。
  - 每个分类器使用约 80% 训练数据，因而输出具有随机差异。
  - 聚合时不是简单多数投票，而是要求“已见”票数超过阈值 `thres`，且 `thres` 明显高于 \(n_k/2\)。该阈值可通过交叉验证选择，用于抵消模型内在的已见类偏置。
- **低维近似**
  - 原始结构化词嵌入维度为 769。
  - 论文发现嵌入实际位于极低维流形上，PCA 后 15 维即可捕获约 0.6224 的
