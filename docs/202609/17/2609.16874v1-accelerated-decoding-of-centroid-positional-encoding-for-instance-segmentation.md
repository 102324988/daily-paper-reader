---
title: Accelerated Decoding of Centroid Positional Encoding for Instance Segmentation
title_zh: 面向实例分割的质心位置编码加速解码
authors: "Carmelo Scribano, Filippo Muzzini, Nedyalko Prisadnikov, Mohammad Mahdi, Yuqian Fu, Giorgia Franchini, Danda Pani Paudel, Marko Bertogna, Luc Van Gool"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.16874v1"
tags: ["query:cv"]
score: 9.0
evidence: 实例分割解码加速
tldr: 实例分割中，将网络原始输出解码为实例掩码的解码阶段常被忽视，却占据可观的执行开销。针对近期基于正弦质心位置编码的方法，其逐像素回归实例质心嵌入，虽无需预定义候选框即可灵活分割，但从稠密嵌入中提取掩码计算代价很高。本文提出面向该编码的优化 CUDA 解码实现，重点处理并行化、同步与 GPU 内存访问。实验表明，该方法显著降低解码开销并改善端到端推理延迟，优于 CPU 和朴素 GPU 实现，凸显编码与解码协同设计对实时视觉系统的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.16874v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16874v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16874v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 597, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.16874v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 596, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.16874v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16874v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 847, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.16874v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1233, \"height\": 542, \"label\": \"Table\"}]"
motivation: 实例分割的解码阶段将原始网络输出转为实例掩码，开销显著却常被忽视，通用CPU或低效GPU实现限制了模型效率收益。
method: 针对正弦质心位置编码，提出定制CUDA解码算法，解决现代GPU上的并行化、同步和内存访问挑战，从稠密质心嵌入中高效提取实例掩码。
result: 相比CPU方案和朴素GPU实现，显著降低解码开销，改善端到端推理延迟，证明高效解码能释放先进输出表示的优势。
conclusion: 强调编码方案与解码算法需协同设计，以支撑实时计算机视觉系统充分利用复杂输出表示。
---

## 摘要
除模型推理之外，将原始网络输出转换为任务级表示的解码阶段也占据了执行开销的很大一部分。尽管预测解码具有实际影响，但它受到的关注相对较少，通常使用通用CPU例程或低效GPU内核实现，从而限制了模型效率进展所带来的收益。本文研究了与近期用于实例分割的正弦质心编码相关的解码开销，其中每个像素回归其所属实例质心的位置嵌入。该方法无需预定义候选区域即可实现灵活分割，但从稠密嵌入中提取实例掩码会带来高计算成本。我们提出了一种针对该编码定制的解码算法的优化CUDA实现，明确解决了现代GPU上并行化、同步和内存访问相关的挑战。我们的解决方案显著降低了解码开销并改善了端到端推理延迟，优于基于CPU的方法和朴素GPU实现。结果表明，高效解码对于充分发挥先进输出表示的优势至关重要，并凸显了为实时计算机视觉系统联合设计编码方案及其解码算法的重要性。

## Abstract
Beyond model inference, the decoding stage, which converts raw network outputs into task-level representations, constitutes a significant portion of the execution cost. Despite its practical impact, prediction decoding has received comparatively little attention and is often implemented using generic CPU routines or inefficient GPU kernels, limiting the benefits of advances in model efficiency. In this work, we investigate the decoding overhead associated with a recent sinusoidal centroid encoding for Instance Segmentation, in which each pixel regresses a positional embedding of its instance centroid. This approach allows flexible segmentation without predefined proposals, but extracting instance masks from dense embeddings incurs a high computational cost. We present an optimized CUDA-based implementation of the decoding algorithm tailored to this encoding, explicitly addressing challenges related to parallelization, synchronization, and memory access on modern GPUs. Our solution significantly reduces decoding overhead and improves End-to-End inference latency, outperforming both CPU-based approaches and naive GPU implementations. The results demonstrate that efficient decoding is essential to fully exploit the advantages of advanced output representations and highlight the importance of jointly designing encoding schemes and their decoding algorithms for real-time computer vision systems.

---

## 论文详细总结（自动生成）

# 论文总结：面向实例分割的质心位置编码加速解码

## 1. 核心问题与整体含义

- **研究动机**：现代计算机视觉系统的研究资源大量投入于模型架构设计与压缩，但**预测解码阶段**（将网络原始输出转换为任务级表示）却长期被忽视。该阶段常以通用 CPU 例程或低效 GPU 内核实现，导致内存受限、同步开销大、加速器利用率低。
- **问题严重性**：解码开销有时甚至主导端到端推理时间，在边缘部署场景（如智慧城市实时视觉）中尤为突出，使得模型效率提升无法转化为系统级的延迟收益。
- **研究对象**：聚焦于近期提出的**正弦质心位置编码**实例分割方法（Prisadnikov 等，2024）。该编码让每个像素回归其所属实例质心的位置嵌入，无需预定义候选框（proposal）即可灵活分割，但**从稠密嵌入中提取实例掩码的计算代价极高**，成为瓶颈。
- **整体含义**：论文主张输出表示（编码方案）与其解码算法应**协同设计**，否则新型表示的理论优势无法在实时系统中落地。

## 2. 方法论

### 2.1 核心思想
针对正弦质心编码的三阶段解码流程（投票直方图 → 局部极大值搜索 → 掩码聚合），设计**专用 CUDA 实现**，重点解决并行化、同步与内存访问三大挑战，并消除朴素实现中的原子操作竞争与内存重用不足问题。

### 2.2 编码与解码形式化
- **编码目标**：对每个空间位置 (i,j)，若属于实例 k，则编码为质心坐标的正弦位置编码拼接 `concat(γ(x_c^k), γ(y_c^k))`，否则为背景零向量；其中 `γ(p) = (sin(2^l πp), cos(2^l πp))`，谐波数 L=4，故每像素 4L=16 维。
- **投票直方图 H**：每个输出位置对候选质心 bin (a,b) 投软票，权重为编码空间可分离距离的负指数 `w_ij(a,b) = e^{-(dx+dy)}`，再对所有像素求和得 H(a,b)。
- **候选质心提取**：在 H 上做局部极大值搜索，得到候选质心集合 C，同时抑制噪声导致的弱候选。
- **掩码聚合**：每个输出像素选择编码空间距离最近的候选质心 m，若距离小于阈值 τ 则 `M̂_m(i,j)=1`。

### 2.3 关键优化技术
- **直方图累积（HistAcc）**：
  - 朴素实现为每个 ((i,j),(a,b)) 元组分配一个线程，并发写入全局直方图需 `atomicAdd`，热门 bin 更新被串行化，造成严重竞争。
  - 本文利用指数可分解性：`w_ij(a,b) = E_x(i,j,a)·E_y(i,j,b)`，将 H 分解为 `H = E_x^T · E_y`。
  - 分别用专用内核计算 E_x、E_y 矩阵（每线程计算一个元素），再用 **cuBLAS 的 `cublasSgemmStrided`** 完成矩阵乘，**彻底消除累加器与原子操作**。
  - 预计算并缓存 x、y 方向的编码调色板 P_w、P_h 作为模型参数，避免重复计算与拷贝；不再显式使用 void encoding，背景像素隐式处理。
- **局部极大值搜索（LMS）**：每个直方图单元一个线程，检查 8 邻域是否为严格局部极大值且超过阈值，输出稀疏二值指示图。
- **掩码聚合（MaskAgg）**：每个输出像素一个线程，先将其亲和度值（E_x、E_y 条目）**加载到共享内存**避免重复全局读取，再在单线程内扫描所有激活质心 bin，选择满足阈值 `e^{-τ}` 中亲和度最高的一个；计数器更新使用 `atomicAdd`，但每次分配像素仅一次，开销可控。

## 3. 实验设计

- **硬件与框架**：NVIDIA **Jetson Orin Nano**，使用 **TensorRT** 加速推理，解码器以 `IPluginV3` 接口实现为 TensorRT 插件；用 `trtexec` 测量执行时间（排除数据传输延迟）；用自定义 CUDA 分配器追踪峰值内存。
- **隔离评估**：设计最小 ONNX 图（图 2），将解码器与模型其余部分解耦，单独编译为 TensorRT 引擎。
- **数据集 / 基准**：任务性能使用 **Panoptic Quality (PQ)** 指标，遵循 [9] 的协议，在 **COCO** 数据集上评估；对比对象为 **PyTorch 参考实现**（CPU/框架级）与**朴素 CUDA 实现** [12]。
- **对比维度**：
  - 直方图 bin 分辨率：8×8、16×16、32×32、64×64、80×80（表 1）。
  - 编码空间特征分辨率：160×160、192×192、256×256、352×352（图 3b，固定 32×32 bins）。
  - 端到端输入分辨率与精度：280×280、336×336、448×448、616×616，精度 FP32/FP16/INT8（表 2）。
  - 逐内核剖析：HistAcc、LMS、MaskAgg、cublasSgemmStrided（表 3）。

## 4. 资源与算力

- 文中明确使用 **NVIDIA Jetson Orin Nano** 作为推理/评测平台，配合 TensorRT 与 cuBLAS。
- **未说明**训练算力（GPU 型号/数量、训练时长等），因为本文工作属于对**已有预训练模型**的解码阶段优化，不涉及重新训练。
- 推理评测配置：`trtexec` 参数为 100 次迭代、100 次平均、10 次预热、静态插件加载。

## 5. 实验数量与充分性

- **实验组数**：约 4 大类实验——bin 分辨率对比（5 档）、特征分辨率扩展性（4 档）、端到端分辨率×精度矩阵（4×3=12 组）、内核级剖析（含朴素 vs 优化两组对比），另有 PQ 精度验证。
- **充分性**：
  - 覆盖了精度-延迟权衡、分辨率可扩展性、端到端影响、内核级瓶颈四个层面，维度较全面。
  - 通过保持模型其余部分一致，仅替换解码器实现，**对比公平**，能有效隔离解码阶段贡献。
  - 同时报告执行时间标准差，验证时序稳定性（安全关键应用关注点）。
- **局限**：PQ 精度对比仅在表 1 中按 bin 分辨率给出，未对不同输入分辨率/精度下的精度做完整交叉验证；对比基线只有 PyTorch 参考与朴素 CUDA，未与其他 GPU 解码优化方案横向比较。

## 6. 主要结论与发现

- 优化解码器在**所有配置下均优于 PyTorch 参考实现**，且时序稳定性显著更好（标准差从毫秒级降至微秒级）。
- PQ 随 bin 分辨率提升呈**边际递减**，在 32×32 处趋于饱和（约 52 PQ），故后续实验采用 32×32。
- 端到端加速最高达 **55%（INT8，280×280）**；输入分辨率越高、精度越低，加速效果越明显（FP32 高分辨率下加速降至约 12%，因骨干网络占比增大）。
- 内核剖析显示：优化后 HistAcc 从 77.99 ms 降至 13.68 ms，MaskAgg 获得约 **6 倍加速**；瓶颈从计算转向**内存操作（占剩余延迟 33%）**。
- H 分解与 cuBLAS 使用是性能提升的主要贡献者。
- **核心洞见**：高效解码对于发挥先进输出表示的潜力不可或缺，编码与解码需协同设计。

## 7. 优点

- **问题切入新颖**：关注被普遍忽视的"解码后处理"瓶颈，选题具有实际工程价值与边缘部署意义。
- **数学洞察驱动优化**：利用指数可分解性将带原子竞争的直方图累积转化为两次矩阵乘（cuBLAS），从算法层面根除竞争，而非仅做工程微调。
- **系统级工程完备**：预计算调色板、共享内存缓存亲和度、静态内存预分配、单线程内评估全部候选质心等，多项优化协同，兼顾性能与稳定性。
- **实验设计严谨**：通过隔离解码器的 ONNX/TensorRT 插件方式，做到控制变量、公平对比；同时报告延迟、内存、精度与稳定性多维指标。
- **可复现性较好**：给出 trtexec 具体参数、内核级耗时明细，便于社区复现与扩展。

## 8. 不足与局限

- **平台单一**：仅在 Jetson Orin Nano（边缘平台）上评测，未在数据中心级 GPU（如 A100/H100）上验证，泛化性存疑。
- **缺乏训练侧信息**：未涉及模型训练成本，端到端收益评估仅限推理阶段。
- **精度验证不完整**：PQ 仅在 bin 分辨率维度对比，缺少输入分辨率、数值精度（FP16/INT8 量化）对精度影响的交叉分析，量化后精度损失风险未量化。
- **对比基线有限**：只对比 PyTorch 参考与自身朴素实现，未与 TensorRT 原生 NMS、其他 GPU 后处理库等主流方案比较。
- **残余瓶颈未解决**：内存操作仍占 33% 延迟，论文将其列为未来工作，当前实现尚未达到最优。
- **适用性受限**：方法高度针对正弦质心编码这一特定表示，对其他实例分割解码范式（如 Mask R-CNN 的 NMS、聚类分组）未必可直接迁移。
- **元数据疑点**：论文标注为 2026 年 9 月发表，且 arXiv 编号与日期存在异常，实际可获取性与同行评审状态需读者自行核实。

（完）
