---
title: Equivariant Filter for High Performance Image Tracking using an Event Camera
title_zh: 使用事件相机的高性能图像跟踪的等变滤波器
authors: "Angus Apps, Yixiao Ge, Timothy L. Molloy, Robert Mahony"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09103v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于等变滤波器的事件相机图像跟踪
tldr: 图像跟踪在自动驾驶和机器人控制中至关重要，但传统方法在高速运动下性能受限。本文针对事件相机，提出一种等变滤波器设计，利用AEB跟踪器提取特征位置测量，并结合特殊欧几里得群对称性计算仿射变换。通过等效测量更新步骤去除了测量间的时间相关性，实现平滑跟踪。在一般和快速旋转运动数据集上，该方法可跟踪速度达7000像素/秒的特征，优于直接优化和协方差交集基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09103v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1469, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09103v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 880, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09103v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 855, \"label\": \"Figure\"}]"
motivation: 事件相机在高速运动下具有优势，但现有图像跟踪方法受测量时间相关性影响，导致性能下降。
method: 提出等变滤波器，融合AEB特征测量和SE(2)群对称性，通过等效测量更新步骤解耦高度相关的特征位置数据。
result: 在高速旋转场景下，平滑跟踪图像平面上速度达7000像素/秒的特征，优于直接优化和协方差交集方法。
conclusion: 等变滤波器有效克服了数据相关性，实现了事件相机下的高性能、平滑图像跟踪，对高速运动场景具有实际应用价值。
---

## 摘要
图像跟踪是估计场景运动图像与原始参考图像之间变换的问题。该问题在自动驾驶车辆或机器人控制中至关重要，因为图像编码了相机或环境的运动信息，同时在纯计算机视觉应用中也很重要。本文提出了一种使用事件相机进行高性能平面图像变换跟踪的等变滤波器设计。该设计利用异步事件斑点（AEB）跟踪器（Wang等，2024）从原始事件流中提取特征位置测量，并利用等变滤波器通过特殊欧几里得群对称性计算仿射图像平移和旋转。等变滤波器包含一个等价测量更新步骤，用于去相关AEB跟踪器提供的（高度时间相关的）特征位置测量。我们使用两个涉及一般运动和快速旋转运动的数据集进行了实验评估。我们将结果与直接优化（从原始斑点轨迹估计相对变换）以及用于克服数据相关性的协方差交集方法进行基准比较。我们的设计为在图像平面上以每秒高达7000像素移动的特征提供了平滑的图像跟踪。

## Abstract
Image tracking is the problem of estimating the transformation that relates a moving image of a scene to an original reference image. The problem is important in control of autonomous vehicles or robots, where the image encodes information about the motion of the camera or environment, as well as in pure computer vision applications. In this paper, we present an equivariant filter design for high performance tracking of planar image transformations using an event camera. The design exploits the Asynchronous Event Blob (AEB) tracker (Wang et al., 2024) to extract feature-position measurements from the raw event stream, and an equivariant filter to compute an affine image translation and rotation using the special Euclidean group symmetry. The equivariant filter incorporates an equivalent-measurement update step that de-correlates the (highly temporally correlated) feature-position measurements provided by the AEB tracker. We evaluate the design experimentally using two datasets involving general and fast rotational motion. We benchmark results against direct optimisation (estimating the relative transformation from the raw blob tracks), and a covariance intersection approach for overcoming data correlation. Our design provides smooth image tracking for features moving up to 7000 pixels per second on the image plane.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：图像跟踪是估计场景运动图像与参考图像之间变换的关键问题，广泛应用于自动驾驶、机器人视觉控制、视觉里程计、图像稳定等任务。传统方法依赖帧间特征点代数关系，但计算出的变换估计是独立的，未利用由真实相机运动引起的图像变换的时间演化特性。
- **背景问题**：现有图像跟踪算法在高度动态场景（高光流导致大特征位移）中特征跟踪困难。事件相机作为一种新传感模态，在高速环境下具有低延迟优势。然而，事件驱动的特征跟踪器（如AEB跟踪器）输出高度时间相关的测量，不能作为后续图像跟踪滤波器的独立测量。
- **本文目标**：提出一个等变滤波器（EqF），用于事件相机的平面图像变换估计。通过级联AEB跟踪器与EqF，并利用等效测量框架去相关，实现高速（最高7000像素/秒）平滑的图像跟踪。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 2.1 核心思想
- 采用**级联滤波设计**：第一级AEB跟踪器处理原始事件流，输出特征位置（带协方差）；第二级EqF利用这些测量估计图像变换（SE(2)上的平移和旋转）。
- 关键创新：引入**等效测量更新**（Equivalent Measurement Update），将AEB跟踪器输出的高度时间相关状态转化为独立分布的虚拟测量，从而在EqF中正确融合。

### 2.2 系统模型
- **状态空间**：`M := SE(2) × se(2)`，包括相机位置/旋转（用SE(2)表示）和体坐标系下的线速度/角速度（用se(2)表示）。
- **测量模型**：特征点在体坐标系中的位置 `p_i^t = h_i(P) = P^{-1} ᵽp_i`，其中ᵽp_i是参考图像中的特征位置。
- **动力学模型**：假设恒定速度，`Ṗ = P(V + U1^∧)`, `V̇ = U2^∧`，输入U1、U2设为零。

### 2.3 等变滤波器设计
- **对称性**：利用半直积群 `G = SE(2) ⋉ se(2)` 作为对称群，定义状态作用、输入作用和系统提升。
- **滤波器步骤**：
  - **预测**：基于动力学模型更新状态估计和协方差。
  - **更新**：使用等效测量，通过扩展卡尔曼更新公式（公式10-14）计算修正量∆，更新状态和协方差。
- 关键矩阵：A_t, B_t, C_t 是局部线性化雅可比矩阵。

### 2.4 等效测量更新
- **问题**：AEB跟踪器输出（状态估计和协方差）是时间相关的，直接使用会导致滤波器发散。
- **方法**：将AEB跟踪器在时间间隔 `[k-m, k]` 内的预测和更新结果反向推导出独立虚拟测量 `ŷ_{k|k-m}` 及其协方差 `̂R_{k|k-m}`，公式为：
  ```
  ŷ = p̂_{k|k-m} + Σ_{k|k-m} (Σ_{k|k-m} - Σ_{k|k})^{-1} (p̂_{k|k} - p̂_{k|k-m})
  ̂R = Σ_{k|k-m} (Σ_{k|k-m} - Σ_{k|k})^{-1} Σ_{k|k-m} - Σ_{k|k-m}
  ```
- **自适应窗口**：当协方差变化太小时，需要累积多个事件（m>1）以保证矩阵可逆。

## 3. 实验设计：数据集、场景、benchmark及对比方法
### 3.1 硬件与场景
- **相机**：Prophesee EVK4 事件相机。
- **两个数据集**：
  1. **一般运动**：手持相机在平行于标记平面的平面上平移和旋转，标记有四个特征点，运动非理想平面（含小振动），测试算法鲁棒性。
  2. **快速旋转运动**：四个特征点置于旋转盘上，由步进电机驱动，转速约4.4转/秒，特征在图像平面速度约7000像素/秒，测试高速性能。

### 3.2 对比方法
1. **直接优化**：利用最小二乘法逐帧求解SE(2)变换，速度通过有限差分计算（零阶保持采样）。
2. **协方差交集（Covariance Intersection, CI）**：同一EqF框架，但更新步骤使用CI融合AEB测量与预测，避免已知相关性问题。

### 3.3 基准与评价
- 定性比较状态估计（SE(2)位置和se(2)速度）的平滑性与噪声水平。
- 定量通过协方差估计（如CI的高估特性）和估计波动幅度比较。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量或训练时长。所有算法均为基于模型（非线性滤波）的方法，无需训练，因此算力需求主要来自在线滤波计算，但不涉及大规模深度学习训练资源。

## 5. 实验数量与充分性
- **实验数量**：两个运动场景（一般运动和快速旋转），每个场景展示完整时间序列和放大窗口的对比，共四组图（图3和图4）。
- **充分性分析**：
  - 场景覆盖了低速/高速、理想平面/非理想平面运动，具有代表性。
  - 对比了两种基线方法（直接优化和协方差交集），均针对相同数据公平比较。
  - 缺乏定量指标（如RMSE、跟踪成功率等），仅凭可视化对比，略显不足。对于真实轨迹的ground truth未明确提供，评价依赖定性平滑性。
  - 未进行消融实验（如不同窗口m值影响、不同特征点数等）。

## 6. 论文的主要结论与发现
- 提出并验证了**等效测量更新**在级联滤波中的关键作用，有效去除了AEB跟踪器的高度时间相关测量。
- EqF+等效测量方法在**平滑性**上显著优于直接优化和协方差交集，尤其在速度估计上表现突出（噪声大幅降低）。
- 对于高速运动（7000像素/秒），滤波方法（EqF）比逐帧优化更稳定，利用时间演化信息。
- 协方差交集保守但稳定，但速度估计噪声较大。
- 等效测量框架为扩展到全单应性（SL(3)）跟踪提供了清晰路径。

## 7. 优点：方法或实验设计上的亮点
- **理论创新**：将等效测量框架应用于事件相机图像跟踪，解决了实际中传感器融合中常见的相关性问题。
- **级联滤波设计**：分别利用AEB的低延迟跟踪能力和EqF的全局结构估计能力，性能互补。
- **高效性**：EqF更新速率可与AEB事件率一致（m=1），也可通过累加降低计算负载。
- **实验设计**：包含低速和高速场景，特别是7000像素/秒的极端运动，展示了算法在极限条件下的鲁棒性。
- **对比公平**：两个对比算法均基于相同AEB跟踪输出，且协方差交集在同一EqF框架下实现，控制变量。

## 8. 不足与局限
- **实验覆盖有限**：仅使用自建数据集，未在公开基准数据集（如事件相机视觉里程计数据集）上测试；缺乏定量误差指标（如相对于ground truth的RMSE）。
- **缺乏消融研究**：未分析不同等效测量窗口m值对结果的影响；未评估特征数量或遮挡对性能的影响。
- **理论假设限制**：假设平面运动（垂直于光轴）且特征点世界静止，应用场景受限；扩展到全单应性（SL(3)）仅提及但未实现。
- **未报告计算开销**：虽然无需训练，但运行时的计算延迟、内存占用等未讨论。
- **无统计显著性**：仅展示单次运行示例（单次手运动/单次旋转实验），缺少多次重复实验的统计结果。
- **定位误差来源**：AEB跟踪器的误差传播和等效测量逆矩阵的数值稳定性未详细分析。

（完）
