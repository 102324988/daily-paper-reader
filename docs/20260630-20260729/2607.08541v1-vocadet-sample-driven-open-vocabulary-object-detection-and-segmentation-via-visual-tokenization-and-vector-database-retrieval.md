---
title: "VocaDet: Sample-Driven Open-Vocabulary Object Detection and Segmentation via Visual Tokenization and Vector Database Retrieval"
title_zh: VocaDet：基于视觉标记化和向量数据库检索的样本驱动开放词汇目标检测与分割
authors: ZhiXin Sun
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08541v1"
tags: ["query:cv"]
score: 9.0
evidence: 基于视觉令牌化的开放词汇目标检测与分割
tldr: 针对开放词汇目标检测与分割中文本提示依赖、扩展性差的问题，提出VocaDet框架，将连续视觉特征离散化为多粒度视觉标记，结合位置去偏和空间拓扑信息构建可扩展的目标记忆库，通过向量检索实现样本驱动的识别，无需模型重训练。在UA-DETRAC数据集上验证了有效性，并支持持续扩展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08541v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 433, \"label\": \"Figure\"}]"
motivation: 现有方法依赖文本提示或有限视觉示例，难以扩展到大型且不断增长的目标库。
method: 使用DINOv3提取特征，经自适应凝聚聚类生成离散视觉标记，与位置去偏和空间拓扑信息一同存入向量数据库，推理时查询匹配。
result: 在UA-DETRAC数据集上无需检测器训练即实现有效开放词汇检测，且随样本积累持续提升。
conclusion: VocaDet通过视觉标记化和向量检索实现了可扩展的开放词汇目标检测与分割。
---

## 摘要
开放词汇目标检测与分割旨在识别预定义类别之外的任意目标。尽管最近的视觉-语言和基于参考的方法显著推动了该领域的发展，但它们通常依赖文本提示、有限的视觉示例或昂贵的特征匹配过程，难以扩展到大规模且持续扩展的目标库。本文提出VocaDet，一种样本驱动的开放词汇目标检测与分割框架，无需模型重新训练，直接从用户提供的正负样本集合中学习目标概念。关键思想是将连续视觉表征转化为离散视觉词汇，并通过可扩展的向量数据库执行高效的基于检索的识别。具体而言，采用DINOv3作为视觉特征提取器，应用具有自适应聚类敏感性的凝聚聚类生成多粒度视觉标记。这些视觉标记与位置去偏表征和空间拓扑信息一起，作为可扩展的目标记忆存储在向量数据库中。推理时，查询图像被转化为视觉标记，并与存储的目标记忆进行高效匹配，以实现目标定位和分割。此外，引入背景过滤机制，以移除频繁出现的背景模式，减少实际固定相机场景中的冗余检索操作。在UA-DETRAC数据集上的实验表明，VocaDet无需传统检测器训练即可实现有效的开放词汇检测性能，同时随着更多正负样本的积累，支持持续可扩展的识别能力。

## Abstract
Open-vocabulary object detection and segmentation aim to recognize arbitrary objects beyond predefined categories. Although recent vision-language and reference-based approaches have significantly advanced this field, they often rely on text prompts, limited visual examples, or expensive feature matching procedures, making them difficult to scale to large and continuously expanding object repositories. In this work, we propose VocaDet, a sample-driven open-vocabulary object detection and segmentation framework that learns object concepts directly from user-provided positive and negative sample collections without model retraining. The key idea is to transform continuous visual representations into discrete visual vocabularies and perform efficient retrieval-based recognition through a scalable vector database. Specifically, we employ DINOv3 as the visual feature extractor and apply agglomerative clustering with adaptive clustering sensitivity to generate multi-granularity visual tokens. These visual tokens, together with position-debiased representations and spatial topology information, are stored as expandable object memories in a vector database. During inference, query images are converted into visual tokens and efficiently matched against the stored object memories for object localization and segmentation. Furthermore, a background filtering mechanism is introduced to remove frequently occurring background patterns and reduce redundant retrieval operations in practical fixed-camera scenarios. Experiments on the UA-DETRAC dataset demonstrate that VocaDet achieves effective open-vocabulary detection performance without conventional detector training, while supporting continuously expandable recognition capability as additional positive and negative samples are accumulated.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有开放词汇目标检测与分割方法（如基于文本提示或参考图像的方法）存在扩展性差、依赖人工标注提示、计算开销大等局限，难以处理大规模、持续增长的目标库。
- **研究动机**：用户常需要从大量正负样本中自动学习目标概念，并在任意图像中高效、准确地检索目标，而传统方法无法满足这一需求。
- **整体含义**：VocaDet 提出一种**样本驱动**（sample-driven）的框架，无需模型重新训练，通过视觉标记化与向量数据库检索实现开放词汇感知，支持持续扩充样本以提升识别能力，为实际工程场景（如固定摄像头监控）提供可扩展的解决方案。

#### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：将连续视觉特征**离散化为多粒度视觉词汇**，结合位置去偏表征与空间拓扑信息构建可扩展的目标记忆库，推理时通过**向量数据库高效检索**匹配目标，而非依赖传统检测器。
- **关键技术细节**：
  - **特征提取**：使用 **DINOv3** 提取密集语义视觉特征。
  - **视觉词汇生成**：对特征应用**凝聚聚类**（agglomerative clustering），并引入**自适应聚类敏感性参数**控制词汇粒度，生成多粒度离散视觉标记。
  - **位置去偏**：采用 INSID3 提出的位置去偏策略，将原始 DINOv3 特征转换为位置无关表征，减少空间偏差对匹配的影响。
  - **拓扑信息存储**：对于同一目标内的多个聚类区域，记录它们之间的**空间连通性**（indexList），形成局部拓扑结构，在检索时同时要求特征相似性与拓扑一致性。
  - **多粒度数据库**：针对不同聚类敏感性设置多个向量数据库，推理时使用相同敏感性聚类后在其对应库中检索。
  - **背景过滤机制**：在固定摄像头场景中，维护**背景特征记忆**，新帧中视觉词汇先与背景库比较，高相似度者被过滤，仅保留前景词汇进入对象库检索；背景库通过匹配并替换策略在线更新。
  - **后处理**：多粒度匹配可能产生重叠框，使用 **NMS**（非极大值抑制）合并得到最终检测框。
- **算法流程**：
  1. 输入图像 → DINOv3 提取特征 → 凝聚聚类生成视觉词汇 → 背景过滤（可选）。
  2. 剩余词汇与向量数据库中存储的正负样本进行相似度检索，同时验证拓扑一致性。
  3. 匹配成功的词汇聚类转化为候选框 → NMS 输出最终检测结果。

#### 3. 实验设计：数据集、基准、对比方法
- **数据集**：仅使用 **UA-DETRAC** 数据集（多目标检测与跟踪基准，车辆场景）。
- **基准**：论文未给出与传统检测器（如 YOLO）或其他开放词汇方法（如 Grounding DINO）的定量对比结果，仅陈述“取得有竞争力的检测性能”。
- **对比方法**：未明确列出对比方法，也未提供定量指标（如 mAP、召回率等）。实验部分主要定性说明性能可随样本扩充提升，并指出一个局限性（相邻同类目标可能被合并）。

#### 4. 资源与算力
- **未明确说明**：论文未提及使用的 GPU 型号、数量、训练时长、推理速度等算力资源。仅指出框架无需训练，推理时需运行 DINOv3 和聚类、检索。

#### 5. 实验数量与充分性
- **实验数量**：只在一个数据集（UA-DETRAC）上评估，**没有消融实验**（如不同聚类敏感性、背景过滤模块的影响等），也没有与其他方法的定量比较。
- **充分性评估**：**不充分**。缺乏定量结果和对比基线，难以客观判断方法的性能优势。实验覆盖范围窄（仅车辆场景），泛化性未验证。论文更像一个技术方案介绍，而非完整的实验验证报告。

#### 6. 论文的主要结论与发现
- VocaDet 无需传统检测器训练即可实现开放词汇目标检测。
- 通过向量数据库积累正负样本，识别性能可**持续改进**（随着样本库扩大，检索准确率提升）。
- 背景过滤机制能有效减少固定场景中的冗余计算。
- 主要限制：空间相邻且外观相似的同类目标易被错误合并为一个检测实例；初始样本有限时存在冷启动问题。

#### 7. 优点：方法或实验设计上的亮点
- **训练无关**：完全无需模型训练，直接利用预训练特征和检索，适合快速部署。
- **持续学习能力**：通过增量式样本库扩展即可提升性能，无需重新训练模型。
- **多粒度与拓扑感知**：利用不同聚类敏感度生成多尺度视觉词汇，并结合空间拓扑约束，增强了目标表示的鲁棒性。
- **背景过滤机制**：针对固定场景的在线背景记忆更新，有效减少检索计算量，提升效率。
- **可扩展架构**：采用 Milvus 等通用向量数据库，易于扩展至大规模样本库。

#### 8. 不足与局限
- **实验严重不足**：缺乏定量对比、消融实验和跨数据集验证，论文科学性较弱。
- **冷启动问题**：初始阶段样本量少时，特征分布不完善，检索效果可能差。
- **合并问题**：相邻同类目标易被误合并为单个实例，缺乏实例级区分能力（如没有依赖空间距离或实例分割头）。
- **对特征提取器依赖**：严重依赖 DINOv3 的判别力，若特征对某些类别区分度不足，性能受限。
- **推理效率**：虽然背景过滤可减少查询次数，但 DINOv3 推理 + 聚类 + 向量检索全流程可能仍有较高延迟，未提供速度指标。
- **应用局限性**：当前仅验证于车辆数据集，开放词汇能力未在多样场景（如室内、自然图像）中测试。

（完）
