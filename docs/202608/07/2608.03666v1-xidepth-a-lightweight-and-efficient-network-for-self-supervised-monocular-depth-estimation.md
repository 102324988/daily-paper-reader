---
title: "XiDepth: a Lightweight and Efficient Network for Self-supervised Monocular Depth Estimation"
title_zh: XiDepth：用于自监督单目深度估计的轻量高效网络
authors: "Elena Izzo, Riccardo Toniolo, Lamberto Ballan"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03666v1"
tags: ["query:cv"]
score: 9.0
evidence: 自监督单目深度估计网络
tldr: "自监督单目深度估计因无需深度标签且相机设置简单，非常适合资源受限设备。现有轻量模型常采用深度可分离卷积与注意力机制，但能耗高且嵌入式环境兼容性差。为此提出XiDepth，基于XiNet算子块增强特征提取，同时保持低计算量和能耗。在KITTI上仅0.8M参数即达最优性能，在树莓派4上FLOPs降低40%、能耗降低35%，验证了实际嵌入式部署的适用性与高效性，为移动机器人等场景提供了可行方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03666v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1799, \"height\": 1040, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03666v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1399, \"height\": 531, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03666v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1405, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03666v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1390, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03666v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1856, \"height\": 318, \"label\": \"Table\"}]"
motivation: 现有轻量模型常用深度可分离卷积与注意力，但能耗高且嵌入式环境兼容性差，难以部署在资源受限设备上。
method: 提出XiDepth网络，基于XiNet算子块构建，在增强特征提取的同时保持极低的计算量、参数量和能耗。
result: "在KITTI基准上仅用0.8M参数即达到最先进性能，FLOPs减少40%、能耗降低35%，显著优于主流方法。"
conclusion: 实验表明XiDepth高效且节能，适合部署于树莓派等嵌入式平台，实现实时深度估计。
---

## 摘要
自监督单目深度估计已成为一种有吸引力的解决方案，用于设计轻量且有效的模型，部署在计算受限设备上，因为它减少了对昂贵深度传感器的依赖。通过消除对地面真值标注的需求并利用单目相机设置的简单性，这种方法实现了低成本的数据收集，并在计算机视觉和机器人等领域具有广泛的适用性。一个关键挑战是在不损害整体性能的情况下实现资源高效的神经网络。最先进的模型通常采用深度可分离卷积和注意力机制；然而，这些函数往往产生高能量成本并在嵌入式环境中面临兼容性问题。为了解决这个问题，我们提出了XiDepth，一种基于XiNet算子块的轻量级架构，旨在增强特征提取，同时保持低计算复杂度和能量需求。在KITTI数据集上，XiDepth以仅0.8M参数实现了最先进的性能。在树莓派4上的测试进一步证实了其适用于真实世界嵌入式应用，与领先方法相比，FLOPs减少40%，能耗降低35%。

## Abstract
Self-supervised monocular depth estimation has emerged as an appealing solution to design lightweight and effective models for deployment on computationally constrained devices due to its reduced reliance on expensive depth sensors. By eliminating the need for ground-truth annotations and leveraging the simplicity of monocular camera setups, this approach facilitates cost-effective data collection and broad applicability across fields such as computer vision and robotics. A critical challenge is achieving resource-efficient neural networks without compromising the overall performance. State-of-the-art models generally adopt depth-wise convolutions and attention mechanisms; however, these functions often incur high energy costs and face compatibility issues in embedded environments. To address this, we propose XiDepth, a lightweight architecture based on the XiNet operator block, designed to enhance feature extraction while maintaining low computational complexity and energy demand. On the KITTI dataset, XiDepth achieves state-of-the-art performance with only 0.8M parameters. Tests on a Raspberry Pi 4 further confirm its suitability for real-world embedded applications, reducing FLOPs by 40% and energy consumption by 35% compared to leading methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：单目深度估计是计算机视觉、自动驾驶、增强现实和机器人等领域的关键任务。传统方法依赖 LiDAR 或立体相机，成本高昂。自监督单目深度估计利用连续帧间的几何约束作为监督信号，无需深度真值标注，大大降低了数据采集成本。
- **核心挑战**：在计算资源受限的嵌入式设备上，如何在保持模型性能的同时实现低参数量、低 FLOPs、低能耗和高兼容性。现有轻量级模型（如 Lite-Mono、R-MSFM）虽参数量较低，但常采用**深度可分离卷积**和**注意力机制**等操作，这些操作在嵌入式环境中能耗高、兼容性差。
- **论文目标**：提出一种基于 XiNet 算子块的轻量高效自监督单目深度估计网络——XiDepth，在不牺牲性能的前提下，显著降低计算开销和能耗，使其适用于真实嵌入式部署场景。

## 2. 论文提出的方法论

### 2.1 核心思想
- 以 **XiNet** 卷积块为基本算子构建编码器，替代传统深度可分离卷积和注意力机制，兼顾特征提取能力与硬件友好性。
- 采用 **U-Net 风格的编码器-解码器架构**，输出多尺度深度图（全分辨率、1/2、1/4）。
- 使用自监督训练策略：结合位姿估计网络（PoseNet），通过相邻帧之间的视图重建构造监督信号，无需深度真值。

### 2.2 编码器设计（XiDepth Encoder）
- 输入图像经过三个阶段的下采样卷积后，送入四个阶段的 XiNet 模块，每个阶段还融合了对原始输入进行级联平均池化得到的特征图 `x_i^AP`，以减少空间分辨率下降造成的信息丢失。
- XiNet 模块的核心计算流程：
  1. 初始使用 stride=2 的 7×7 卷积快速下采样；
  2. 内部为两层卷积块：`SiLU(BN(Conv(CompConv(z_{l-1})) + s_l))`；
  3. 其中 `s_l` 由自适应平均池化和 1×1 压缩卷积生成，作为通道级缩放信号；
  4. 整个模块不含深度可分离卷积、空洞卷积等非标准操作，保证了嵌入式平台的兼容性。

### 2.3 解码器设计
- 三个解码阶段，每个阶段由**双线性上采样** + 卷积组成；
- 上采样时将编码器对应层特征与当前输入拼接，传递给下一阶段和预测头，输出多尺度逆深度图。

### 2.4 损失函数
- 总体损失为多尺度（s = 1, 1/2, 1/4）下的**光度重投影损失**与**边缘感知平滑损失**之和的均值；
- 光度损失采用 SSIM 与 L1 的组合，取前后帧重建的最小值以处理遮挡和运动物体，并使用二值掩码去除违反相机运动假设的像素；
- 平滑损失使用经均值归一化的逆深度，避免深度趋近于零，提高训练稳定性。

## 3. 实验设计

### 3.1 数据集与基准
| 数据集 | 用途 | 说明 |
|--------|------|------|
| KITTI | 主实验（训练/验证/测试） | 39,810 个单目三元组训练，4,424 验证，697 测试；采用 Eigen split；图像分辨率 640×192；深度评估范围限制在 [0, 80] m |
| Make3D | 泛化能力测试 | 134 张户外高分辨率 RGB 测试图像，直接用 KITTI 上训练的模型推理，不做微调 |

### 3.2 对比方法
- **Monodepth2**（14.3M 参数，全 CNN）
- **R-MSFM3 / R-MSFM6**（3.5M / 3.8M 参数，全 CNN）
- **Lite-Mono**（3.1M 参数，CNN-Transformer 混合）

### 3.3 评估维度
- **性能指标**：Abs Rel、Sq Rel、RMSE、RMSE log、δ < 1.25、δ < 1.25²、δ < 1.25³
- **效率指标**：参数量、FLOPs、推理时间、能耗（mWh）、RAM 使用率、CPU 使用率（在树莓派 4 上实测）

## 4. 资源与算力

- **论文未明确说明所使用的 GPU 型号、数量及具体训练算力资源**。
- 仅提供了训练配置信息：
  - 训练 300 epochs；
  - batch size 为 12；
  - 使用 AdamW 优化器，权重衰减 1e−2；
  - 学习率 5e−4，余弦调度；
  - 数据增强包括水平翻转、亮度/饱和度/对比度调整和色调抖动。
- 嵌入式实测环境为 **Raspberry Pi 4**（无主动散热、无其他用户进程），能耗通过外接电流计测量。

## 5. 实验数量与充分性

### 已开展实验（共 3 组）
1. **KITTI 主实验**：与 4 种主流方法对比 7 项深度估计指标 + 参数量；
2. **树莓派 4 嵌入式效率实验**：对比推理时间、能耗、RAM、CPU、FLOPs；
3. **Make3D 跨数据集泛化实验**：对比 4 种方法（注意：XiDepth 仅用 KITTI 训练，而对比方法使用了 ImageNet 预训练 + KITTI）。

这是本论文的元数据部分，请根据给出的内容继续分析。

### 实验充分性分析
- **不足**：论文**没有开展消融实验**，无法量化 XiNet 模块、多尺度特征融合（`x_i^AP`）、损失函数各组件等对最终性能的贡献。
- **公平性问题**：
  - Make3D 泛化实验中，对比方法使用了额外的大规模 ImageNet 预训练数据，XiDepth 仅使用 KITTI 数据，训练数据不对等，使得对比在某种程度上不够公平（但同时也展示了无预训练下的竞争力）；
  - KITTI 主实验中所有方法均在同一设置下比较，相对公平。
- **总体评价**：实验覆盖了精度、效率、泛化三个维度，方向合理，但在深度分析（如不同输入分辨率、不同骨干网络、不同嵌入平台）和消融验证方面不够充分。

## 6. 论文的主要结论与发现

- XiDepth 在 KITTI 上以 **0.8M 参数**取得了与最先进方法相当甚至更优的深度估计精度，例如 Abs Rel 为 0.120、δ < 1.25 为 0.870，全面优于 Monodepth2 和 R-MSFM，与 Lite-Mono 相当或略优。
- 在树莓派 4 上，XiDepth 相比最优的对比方法（Lite-Mono）：
  - 推理时间降低 35%；
  - 能耗降低 35%；
  - FLOPs 降低 40%；
  - RAM 和 CPU 占用也有小幅降低。
- 在 Make3D 上，即使仅使用 KITTI 训练而未利用 ImageNet 预训练，XiDepth 仍能取得有竞争力的泛化性能（Abs Rel 0.351），优于 DDVO，但与使用 ImageNet 预训练的方法存在差距。
- 总体结论：XiDepth 验证了基于 XiNet 的轻量架构在自监督单目深度估计任务上的可行性和高效性，非常适合嵌入式部署场景。

## 7. 优点

- **极致的轻量化设计**：仅 0.8M 参数，比同类方法少 4~18 倍，同时保持较高的深度估计精度；
- **硬件友好**：避免使用深度可分离卷积、空洞卷积和注意力机制等非标准/高能耗算子，保证了嵌入式设备的兼容性，实测能耗和推理时间显著降低；
- **自监督策略设计严谨**：采用了最小光度损失、自动掩码、边缘感知平滑等多尺度训练技巧，补偿了轻量网络容量不足的问题；
- **多维度评估**：不仅比较精度指标，还实测了嵌入式平台上的真实推理时间、能耗、内存和 CPU 占用，验证了实用价值；
- **跨数据集泛化验证**：在 Make3D 上验证了模型在不同场景下的适应能力，且未使用额外预训练数据，更能体现模型本身的泛化能力；
- **推理阶段无需位姿网络**，进一步降低了实际部署时的计算负担。

## 8. 不足与局限

- **缺少消融实验**：未单独验证 XiNet 块、多尺度池化特征融合、损失函数各项（如掩码、平滑权重 λ）的贡献，难以判断各组件对性能和效率的实际影响；
- **实验平台单一**：嵌入式验证仅在树莓派 4 上进行，未覆盖其他常见嵌入式平台（如 Jetson、手机端 NPU 等），结论的普适性有待验证；
- **Make3D 对比不够公平**：对比方法使用了 ImageNet 预训练而 XiDepth 没有，虽然论文指出了这一点，但缺少在相同预训练条件下的对比；
- **精度提升有限**：在 KITTI 上虽然 δ < 1.25 和 RMSE 达到最佳，但 Sq Rel 指标（0.941）明显差于 Lite-Mono（0.876），说明在某些误差分布上仍有短板；
- **无高分辨率或不同分辨率实验**：仅在 640×192 下验证，未测试更高输入分辨率下的性能与效率权衡；
- **未报告训练成本与部署细节**：缺少 GPU 训练时间、模型转换/量化对效率和精度的影响等工程化细节；
- **应用场景局限性**：仅在驾驶场景（KITTI）和户外场景（Make3D）上验证，未涉及室内场景（如 NYUv2）或动态物体密集场景。

（完）
