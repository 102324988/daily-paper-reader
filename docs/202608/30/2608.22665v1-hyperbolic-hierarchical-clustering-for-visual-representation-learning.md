---
title: Hyperbolic Hierarchical Clustering for Visual Representation Learning
title_zh: 用于视觉表征学习的双曲层次聚类
authors: "Jianan Wei, Guikun Chen, Zhiyuan Weng, Chunchao Guo, Yujia Wang, Wenguan Wang"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22665v1"
tags: ["query:cv"]
score: 8.0
evidence: 通过双曲层次聚类token混合器进行视觉表征学习
tldr: 视觉主干中的token mixer通常采用卷积、注意力或MLP，但黑盒特性缺乏可解释性。本文提出ClusterMixer，将token混合显式建模为层次聚类，并在双曲空间中进行以拟合视觉数据固有的树状结构。基于此构建HCFormer骨干网络，在图像分类、检测、分割等任务上一致超越同类方法，兼具透明性与高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22665v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 570, \"height\": 416, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22665v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22665v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 878, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22665v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 1612, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22665v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 777, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22665v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1282, \"height\": 886, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22665v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1245, \"height\": 654, \"label\": \"Table\"}]"
motivation: 现有token mixer（卷积/注意力/MLP）虽能平衡精度与成本，但编码过程不透明，缺乏可解释性。
method: 提出ClusterMixer，将token混合转化为双曲空间中的层次聚类，并设计HCFormer集成该模块与多种聚类策略。
result: 在图像分类、物体检测、实例分割和语义分割等任务上，HCFormer持续优于现有对应模型。
conclusion: 双曲层次聚类为可解释视觉骨干提供了新范式，兼顾透明性与性能。
---

## 摘要
我们通过重新审视聚类这一机器学习中最经典的方法之一，来研究视觉主干网络中的令牌混合器。有效的令牌混合器是现代视觉主干网络（如视觉Transformer）的基本组成部分，能够促进图像块之间的信息交换。主流的令牌混合器依赖卷积、注意力、MLP或其混合形式，主要聚焦于在准确性和计算成本之间进行权衡。然而，这些方法的一个显著缺点是它们的黑盒性质；其编码过程不透明且缺乏可解释性。与这些不透明的设计不同，我们引入了ClusterMixer，这是一种透明的令牌混合器，它基于聚类范式，并在设计上具有可解释性。ClusterMixer通过层次聚类机制显式地表述令牌混合过程。为了建模视觉数据中固有的自然树状关系，聚类在双曲空间中进行，该空间非常适合以低失真嵌入层次结构。基于这一创新，我们提出了HCFormer，这是一种新的主干架构，它将ClusterMixer与一系列精心设计的聚类策略相结合，以确保在不同任务上的稳健性能。大量实验表明，HCFormer在多种任务中始终优于其对应方法，包括图像分类、目标检测、实例分割和语义分割。考虑到其透明性和有效性，我们希望HCFormer能够促进向可解释主干的范式转变。

## Abstract
We investigate the token mixer in vision backbones by revisiting clustering, one of the most classic approaches in machine learning. An effective token mixer is a fundamental component of modern vision backbones like vision Transformers, facilitating information exchange between image patches. Mainstream token mixers, which rely on convolution, attention, MLP, or their hybrids, primarily focus on navigating the trade-off between accuracy and computational cost. However, a significant drawback of these methods is their black-box nature; their encoding process is opaque and lacks interpretability. Diverging from these opaque designs, we introduce ClusterMixer, a transparent token mixer that is grounded in a clustering paradigm and interpretable by design. ClusterMixer explicitly formulates the token mixing process through a hierarchical clustering mechanism. To model the natural, tree-like relationships inherent in visual data, the clustering is performed in hyperbolic space, which is well-suited for embedding hierarchies with low distortion. Building on this innovation, we present HCFormer, a new backbone architecture that integrates ClusterMixer with a series of meticulously designed clustering strategies to ensure robust performance across tasks. Extensive experiments demonstrate that HCFormer consistently outperforms its counterparts across diverse tasks, including image classification, object detection, instance segmentation, and semantic segmentation. Considering its transparency and efficacy, we hope HCFormer can facilitate a paradigm shift toward interpretable backbones.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：视觉主干网络（如 ConvNet、ViT）中的 token mixer 是信息交换的关键组件。主流的 token mixer 依赖卷积、自注意力、MLP 或混合形式，虽在精度与成本之间取得平衡，但其编码过程是“黑盒”，缺乏可解释性。
- **核心问题**：如何设计一种既高效又具有内在可解释性的 token mixer，同时能够建模视觉数据中天然的层次（树状）结构。
- **整体含义**：论文提出将聚类这一经典机器学习方法重新引入视觉表示学习，并在双曲空间中执行层次聚类，从而构建一个透明、可解释且性能优异的视觉主干网络 HCFormer，试图推动视觉骨干从“黑盒”向“可解释”范式转变。

## 2. 方法论

- **核心思想**：将 token mixing 显式建模为层次聚类过程，并利用双曲几何的树状结构建模能力，在窗口级聚类中使用双曲距离，在补丁级聚类中使用欧氏距离，兼顾效率与语义表达。
- **关键模块：ClusterMixer**
  - **中心估计**：对邻近 patch 特征做平均池化得到聚类中心，无学习参数，计算复杂度为线性。
  - **簇分配**：采用软聚类（soft assignment），通过 Softmax 计算每个 token 分配到各中心的概率，包含可学习的缩放因子 α 和相对位置偏置 B。
  - **特征聚合与传播**：根据分配矩阵，先聚合每个簇内特征，再通过全连接层更新 token 表示。
- **层次聚类策略**：
  - **Patch 级聚类**：在局部非重叠窗口内执行，处理细粒度局部信息。
  - **Window 级聚类**：将每个窗口视为一个数据点，全局聚类，捕获跨窗口的全局上下文。
  - 该设计将二次复杂度降为线性复杂度，避免 shifted window 的局限性。
- **双曲空间建模**：
  - 使用 Lorentz 模型（双曲面模型），通过指数映射将欧氏特征投影到双曲空间。
  - 相似度度量采用 Lorentzian 距离。
  - 数值稳定性处理：使用可学习缩放参数吸收曲率变化，并在映射前裁剪特征范数。
- **整体网络 HCFormer**：基于 MetaFormer 金字塔架构，四个阶段逐步降低 token 数量，ClusterMixer 作为 token mixer 嵌入每个 block。

## 3. 实验设计

- **数据集与场景**：
  - **图像分类**：ImageNet-1K（1.3M 训练图像，1K 类）。
  - **语义分割**：ADE20K（20K 训练图像，150 类）。
  - **目标检测与实例分割**：MS COCO 2017（118K 训练图像）。
- **对比方法**：
  - 分类：ResNet、ViT、DeiT、Swin、PVT、MLP-Mixer、ResMLP、ConvMixer、CoC、FEC 等。
  - 分割：ResNet、PVT、CoC、FEC。
  - 检测：ResNet、PVT、CoC、FEC。
- **评价指标**：Top-1/Top-5 准确率、mIoU、AP box、AP mask，并报告参数量、FLOPs、吞吐量。

## 4. 资源与算力

- 文中明确提到实验在 **4 台 A100 GPU** 上运行（分类、分割、检测均如此）。
- 图像分类：batch size 256，训练 310 epochs，动量 0.9，权重衰减 0.05，学习率 1e-3。
- 语义分割：batch size 16，训练 80k iterations，AdamW 优化器。
- 目标检测：batch size 16，训练 12 epochs（1× schedule），AdamW 优化器。
- 未明确给出总训练时长（小时数）或 GPU 使用总数（例如 4×A100 具体型号未说明），也未提供能耗等更细信息。

## 5. 实验数量与充分性

- 实验覆盖四个主流视觉任务：分类、语义分割、目标检测、实例分割，场景较全面。
- 模型规模丰富：HCFormer 提供了 Nano（5.1M）、Tiny（7.0M）、Small（16.1M）、Medium（33.7M）四个变体，与不同规模 baselines 对比。
- 消融实验充分，包含：
  - 层次聚类 vs. shifted windows；
  - 双曲 vs. 欧氏距离，以及混合几何的对比；
  - 曲率 κ 的影响；
  - 窗口级聚类中心数量；
  - 关键组件（层次聚类、双曲几何、相对位置偏置）的贡献。
- 还有聚类可视化分析。
- 整体上实验数量较多、对比公平（同参数规模、同训练设置），能够支撑结论的可靠性。

## 6. 主要结论与发现

- HCFormer 在图像分类上优于同参数主流模型：例如 HCFormer-Medium 以 33.7M 参数达到 82.4% Top-1，超过 Swin-Tiny（81.3%），且比现有聚类方法 CoC/FEC 提升 1.2~3.3%。
- 在语义分割中，HCFormer-Small 以 18.9M 参数取得 40.4% mIoU，接近 CoC-Medium（25.2M）和 FEC-Large（31.9M）。
- 在目标检测/实例分割上，HCFormer 同样优于同规模聚类方法，且吞吐量更高。
- 消融实验证实了层次聚类、双曲几何和相对位置偏置三者互补，缺一不可。
- 双曲几何更适合窗口级（抽象层级）聚类，而欧氏几何更适合 patch 级（局部细节）聚类。

## 7. 优点

- **可解释性**：基于聚类的 token mixer 天然可解释，聚类过程透明，可视化分析直观。
- **性能优异**：在多个任务上超越同规模主流方法，证明可解释模型不必牺牲性能。
- **效率提升**：层次聚类将复杂度降至线性，且比 shifted windows 更适合聚类式 token mixer。
- **双曲几何的合理运用**：将欧氏空间用于局部平坦结构、双曲空间用于全局层次结构，取长补短。
- **无参数中心估计**：利用平均池化避免额外学习参数，减轻过拟合风险。
- **软聚类设计**：相对硬聚类（如 CoC）能更灵活地融合多簇信息，提升表达力。

## 8. 不足与局限

- **算力信息不完整**：未报告训练时间（GPU 小时）、能耗等资源成本，难以全面评估效率。
- **模型规模有限**：最大模型仅 Medium（33.7M），未验证更大规模（如 Base/Large）下的表现，无法评估扩展性。
- **部分对比缺失**：与最先进的基于注意力/MLP 的大模型（如 Swin-Base、ViT-Large）相比，HCFormer 的规模断层，直接比较不够全面。
- **双曲几何引入的数值稳定性依赖**：论文依赖可学习缩放和特征裁剪来保证稳定，但未深入分析这些选择的敏感性。
- **下游任务多样性仍有限**：未覆盖视频、3D 点云、多模态等更广泛视觉场景。
- **可视化分析较简略**：只展示了少量聚类图，缺乏定量评估可解释性的指标或用户研究。

（完）
