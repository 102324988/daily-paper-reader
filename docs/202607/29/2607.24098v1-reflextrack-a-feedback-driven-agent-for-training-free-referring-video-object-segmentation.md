---
title: "ReflexTrack: A Feedback-Driven Agent for Training-Free Referring Video Object Segmentation"
title_zh: ReflexTrack：一种反馈驱动的免训练指代视频目标分割智能体
authors: "Yuanjia Li, Tianyang Xu, Tao Zhou, Zhangyong Tang, Xiao-Jun Wu, Josef Kittler"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24098v1"
tags: ["query:cv"]
score: 9.0
evidence: 引用视频目标分割属于图像分割技术
tldr: "针对无需训练的指代视频对象分割中初始空间定位和时序传播缺乏验证的问题，提出ReflexTrack，一种反馈驱动智能体。它通过掩码引导的空间细化迭代优化关键帧提示，并利用视频级掩码反射定位不可靠区间，通过互补关键帧重传播生成候选预测，仅采纳经验证的改进。所有组件冻结推理，在Ref-VPS和ReasonVOS上取得69.7 Q和67.2 J&F，证明了预测级反馈的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24098v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1770, \"height\": 943, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24098v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1778, \"height\": 979, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24098v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 870, \"height\": 890, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24098v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 861, \"height\": 385, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24098v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 543, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24098v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 402, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24098v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 292, \"label\": \"Table\"}]"
motivation: 现有无需训练的RVOS方法依赖单次空间定位和掩码传播，初始提示和时间预测缺乏验证，导致可靠性不足。
method: 提出空间细化（掩码引导迭代更新bbox和正负点）与视频级反射（定位不可靠区间并选择互补关键帧重传播，仅采纳验证通过的候选）。
result: "在Ref-VPS上Q分数69.7，ReasonVOS上J&F分数67.2，优于现有无需训练方法。"
conclusion: 预测级反馈闭环显著提升了无需训练RVOS的可靠性，无需额外训练即可取得竞争性性能。
---

## 摘要
指代视频目标分割（RVOS）要求在整个视频中分割由自然语言指定的目标。最近的智能体方法结合多模态大语言模型与可提示分割模型，无需特定任务训练即可执行RVOS。然而，大多数流程依赖于一次性空间定位后接掩码传播，导致初始提示和时间预测基本未经验证。我们提出ReflexTrack，一种免训练、反馈驱动的智能体，在空间和时间两个层面形成闭环。掩码引导的空间细化评估当前关键帧提示生成的掩码，并迭代更新边界框及正负点，从而产生更可靠的初始化。视频级掩码反思评估完整掩码序列，定位不可靠区间，选择互补修复关键帧，并通过掩码引导的再传播生成候选预测。仅采用经验证有改进的候选预测来更新受影响的区间，从而保留其他位置的可靠预测。所有组件在推理期间保持冻结。ReflexTrack在Ref-VPS上达到69.7的总体$\mathcal{Q}$分数，在ReasonVOS上达到67.2的$\mathcal{J}\&\mathcal{F}$分数。这些结果表明，预测级反馈显著提高了免训练RVOS的可靠性。

## Abstract
Referring video object segmentation (RVOS) requires segmenting a target specified by natural language throughout a video. Recent agentic approaches combine multimodal large language models with promptable segmentation models to perform RVOS without task-specific training. However, most pipelines rely on one-shot spatial grounding followed by mask propagation, leaving both the initial prompts and temporal predictions largely unverified. We introduce ReflexTrack, a training-free, feedback-driven agent that closes this loop at both spatial and temporal levels. Mask-guided Spatial Refinement evaluates the mask induced by the current keyframe prompt and iteratively updates the bounding box together with positive and negative points, yielding a more reliable initialization. Video-level Mask Reflection assesses the complete mask sequence, localizes unreliable intervals, selects complementary repair keyframes, and generates candidate predictions through mask-guided re-propagation. Only candidates that provide a verified improvement are used to update the affected intervals, preserving reliable predictions elsewhere. All components remain frozen during inference. ReflexTrack achieves an overall $\mathcal{Q}$ score of $69.7$ on Ref-VPS and a $\mathcal{J}\&\mathcal{F}$ score of $67.2$ on ReasonVOS. These results demonstrate that prediction-level feedback substantially improves the reliability of training-free RVOS.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有的免训练指代视频目标分割（RVOS）方法通常采用“一次性空间定位 → 掩码传播”的流水线，初始空间提示（如边界框）和时序预测结果缺乏显式验证。这导致当初始框不够精确或传播中发生遮挡、形变、快速运动等问题时，错误会累积，造成目标丢失、漂移和时序不一致。
- **整体含义**：本文提出 **ReflexTrack**，一种反馈驱动的免训练智能体，通过引入**空间级**和**视频级**的两层闭环反馈，让模型在生成预测后能自动评估并修复问题，从而显著提升免训练 RVOS 的可靠性。

---

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将传统的“推理→定位→分割”单向流程扩展为“推理→定位→分割→评估→修复”的闭环。反馈作用于两个互补层面：
  - **空间层面**：对关键帧掩码进行评估，迭代优化边界框、正点和负点；
  - **时序层面**：对完整掩码序列进行质量评估，定位低质量区间，通过重选关键帧和重传播生成修复候选，并仅采纳经验证的改进。
- **关键技术细节**：
  1. **初始目标定位**：选取代表性关键帧，利用 MLLM 和视觉定位模块生成初始空间提示（如边界框）。
  2. **掩码引导的空间细化**（Mask-guided Spatial Refinement）：
     - 对当前提示生成的掩码，由评估代理检查目标正确性和完整度，并诊断错误类型（如截断、背景泄漏、边界模糊）。
     - 细化代理据此更新提示：调整边界框（收紧/扩展/移位）、添加正点（强化目标区域）、添加负点（抑制背景和干扰物）。
     - 循环迭代直至掩码合格或达最大次数，最终提示初始化 SAM 3 的视频传播。
  3. **视频级掩码反射**（Video-level Mask Reflection）：
     - **自适应帧采样**：选取端点帧、均匀帧和随机帧构成评估集，降低计算开销。
     - **质量评估**：VLM 评估每帧的掩码正确性、完整性、时序一致性，输出序列得分和低质量区间。
     - **修复关键帧推理**：对每个低质量区间，修复代理分析失败原因，从全视频中重新选择关键帧（不局限于错误区间），重构更丰富的文本上下文。
     - **掩码引导的再传播**：利用空间细化后的提示，通过 SAM 3 双向传播生成多个候选序列。
     - **修复验证与选择性更新**：比较初始序列和所有候选，仅当候选质量显著提升（≥阈值 δ）时替换对应区间，并迭代进行新一轮评估直至收敛。
- **公式与算法流程**（文字说明）：
  - 空间细化：$M^{(r)}_k = S(I_k, P^{(r)}_k)$ → 评估 → 若失败则 $P^{(r+1)}_k = \mathcal{R}_{sp}(I_k, q, P^{(r)}_k, M^{(r)}_k, z^{(r)}_k)$。
  - 视频反射：采样 $S_{eval}$ → 质量评估得到低质区间 $B_j$ → 修复关键帧推理 → 生成 $C$ 个候选 → 最大化评估函数选择 $c^*_j$，仅当 $\mathcal{E}(M^{(c^*_j)}) > \mathcal{E}(M^{(0)}) + \delta$ 时替换。

---

## 3. 实验设计

- **数据集**：
  - **Ref-VPS**（新基准），采用总体质量分数 $\mathcal{Q}$。
  - **ReasonVOS**，采用标准 $\mathcal{J}\&\mathcal{F}$、区域相似度 $\mathcal{J}$、轮廓准确度 $\mathcal{F}$。
- **对比方法**：
  - Ref-VPS：MUTR、UNINEXT、VD-IT、GLUS、REM（MS-1.4B & Wan-14B）。
  - ReasonVOS：VideoLISA、GLUS、RGA3、CoT-RVS（GPT-4o）、Refer-Agent。
- **消融实验**：逐步添加“空间细化”和“视频反射”组件，并替换 MLLM 为 Qwen3.7-Plus 或 GPT-5.5。

---

## 4. 资源与算力

- **文中明确提到**：所有实验在**单张 NVIDIA RTX 3090 GPU** 上完成。未提及训练时长（因为整个流程免训练，仅在推理时使用 MLLM 和 SAM 3）。

---

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：两个数据集分别与 5~6 种方法对比（表 1、表 2）。
  - 消融实验：共 4 组配置（Base, Base+GR, Base+GR+Mask Reflection (Qwen), Base+GR+Mask Reflection (GPT)），展示各组件贡献（表 3）。
  - 定性分析：提供与 Refer-Agent 的视觉对比，以及空间细化后的提示可视化。
- **充分性与公平性**：
  - 对比方法涵盖有监督、零样本、免训练等多种类型，且引用了最新工作（如 Refer-Agent）。
  - 消融实验清晰分离了空间反馈和时序反馈的独立贡献，验证了二者互补。
  - 实验结果可复现（免训练、使用公开模型）。结论客观。

---

## 6. 论文的主要结论与发现

- **主要结论**：在免训练 RVOS 中引入**预测级反馈**（空间级 + 视频级）显著提升了可靠性和整体性能。
- **具体发现**：
  - 空间细化单独带来 6.4 个 $\mathcal{Q}$ 点的提升（54.2 → 60.6）。
  - 视频反射进一步带来 4.1 个点提升（60.6 → 64.7）。
  - 使用更强 MLLM（GPT-5.5）将 $\mathcal{Q}$ 推至 69.7，总提升 15.5 点，相对增益 28.6%。
  - 在 ReasonVOS 上取得 67.2 $\mathcal{J}\&\mathcal{F}$，区域相似度 $\mathcal{J}$ 高达 70.1，表明空间细化有效改善目标覆盖。

---

## 7. 优点

- **免训练**：所有组件（MLLM、SAM 3）保持冻结，无需任务特定训练或额外标注。
- **模块化与可替换**：空间细化和视频反射可独立集成，且 MLLM 可通过更强的模型直接提升性能。
- **反馈闭环**：首次将预测级验证纳入免训练 RVOS 流程，弥补了现有方法缺乏验证的缺陷。
- **选择性修复**：视频反射仅替换低质量区间，保留高质量预测，避免过度计算。
- **鲁棒性强**：定性结果展示了对变形、动态、非刚体目标的有效处理。

---

## 8. 不足与局限

- **依赖 MLLM 质量**：性能受限于所用 MLLM 的推理能力（如 GPT-5.5 优于 Qwen3.7-Plus），较弱 MLLM 下可能效果有限。
- **计算开销**：多次调用 MLLM（评估、诊断、细化、修复推理等）会导致推理延迟增加，不利于实时应用。
- **未讨论失败案例**：论文未分析在哪些场景下反馈机制仍可能失败（如严重遮挡、目标消失时间过长）。
- **δ 阈值设定**：修复验证中的最小增益 δ 是人工设定的超参数，缺乏自动调整策略，可能影响公平性。
- **跨域泛化**：仅在 Ref-VPS 和 ReasonVOS 两个数据集上评估，未在其他视频分割基准（如 MeViS、DAVIS）上验证。
- **代码与复现**：论文未直接提供代码或模型权重，可复现性有待社区验证。

（完）
