---
title: Few-Shot Open-Vocabulary Remote Sensing Segmentation via Textual Inversion
title_zh: 基于文本反转的少样本开放词汇遥感分割
authors: "Junhyuk Heo, Junghwan Park"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25563v1"
tags: ["query:cv"]
score: 9.0
evidence: 遥感开放词汇分割
tldr: 开放词汇分割在遥感图像上对部分类别性能显著低于地面图像，根源在于类别名称在视觉-语言嵌入空间中定位不佳。通过文本反演从少量示例中学习更优的文本嵌入，无需修改模型参数，推理仅依赖文本。该方法使受影响类别的mIoU从3.9提升至39.4，在八个遥感数据集上超越其他注入视觉提示的小样本方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 996, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1276, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1256, \"height\": 925, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 585, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 561, \"height\": 459, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1271, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25563v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25563v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1183, \"height\": 441, \"label\": \"Table\"}]"
motivation: 遥感开放词汇分割中类别名称在嵌入空间定位弱，导致性能差距，而自然语言改述无法弥补。
method: 冻结分割模型，通过文本反演从少量示例学习优化的文本嵌入，推理时仅使用文本查询。
result: 受影响类别mIoU从3.9提升至39.4，在八个遥感数据集上优于其他小样本方法。
conclusion: 优化文本查询可有效弥补遥感开放词汇分割的性能差距，无需视觉提示干预。
---

## 摘要
开放词汇分割能够通过文本查询对任意类别进行标注而无需逐个类别训练，但在遥感影像上，对于在其他场景中可靠处理的类别，其表现不佳。我们发现，这一差距很大程度上源于文本查询本身，而非分割模型。由于这些模型并非专门针对俯拍影像设计，作为查询的类别名称往往是视觉-语言嵌入空间中的弱地址。我们表明，更好的名称能够部分弥补这一差距，而剩余的失败则需要一种经过测试的自然语言改写无法提供的地址。我们通过冻结模型上的文本反转从少量示例中恢复该地址，并保持推理时仅使用文本。在一个代表性基准上，该方法将受影响类别的平均交并比从3.9提升至39.4，并在八个遥感数据集上优于那些在推理时注入视觉提示的少样本方法。

## Abstract
Open-vocabulary segmentation labels arbitrary categories from a text query without per-class training, yet on remote sensing imagery it underperforms on categories it handles reliably elsewhere. We find that much of this gap traces to the text query rather than to the segmentation model. Because these models are not specialized for overhead imagery, the class name that serves as the query is often a weak address into the vision-language embedding space. We show that a better name repairs part of the gap, while the remaining failures call for an address that the tested natural-language rephrasings do not provide. We recover that address from a few examples through textual inversion on a frozen model, keeping inference text only. On a representative benchmark this raises the mean intersection over union on the affected categories from 3.9 to 39.4, and across eight remote sensing datasets it improves over few-shot methods that instead inject visual prompts at inference.

---

## 论文详细总结（自动生成）

# 论文总结：基于文本反转的少样本开放词汇遥感分割

## 1. 核心问题与研究动机
- **背景**：开放词汇分割模型能够通过文本查询分割任意类别，无需逐类别训练。该类模型在自然场景中表现良好，但在遥感影像上，部分类别（如“游泳池”、“裸土”）的性能却显著低于其在地面图像上的表现。
- **核心问题**：作者发现性能差距的主要来源是**文本查询本身**，而非分割模型。由于视觉-语言模型并非专为俯拍遥感影像设计，标准类别名称（如“swimming pool”）在嵌入空间中往往是一个弱地址（weak address），难以与遥感图像特征对齐。
- **研究动机**：探究能否通过改进文本查询来弥补这一差距。自然语言改述（如用“pool”代替“swimming pool”）只能部分修复，剩余失败需要更优的嵌入地址。本文提出利用**文本反转**（textual inversion）从少量示例中学习优化的文本嵌入，从而使推理时仍仅依赖文本，无需修改模型参数或注入视觉提示。

## 2. 方法论
### 核心思想
- 冻结预训练的开放词汇分割模型（如CLIP-based模型），不更新其参数。
- 通过**文本反转**，从少量（如1~5张）遥感图像示例中学习每个目标类别对应的优化文本嵌入向量（learned token embedding）。
- 推理时，仅将学习到的文本嵌入作为查询输入分割模型，无需任何视觉提示（如参考图像、掩码）。

### 关键技术细节
1. **文本反转（Textual Inversion）**：  
   - 为每个目标类别定义一个可学习的伪词嵌入（pseudo-word token），初始化为类别名称对应的CLIP文本嵌入。
   - 使用少量示例图像的**分割掩码**，通过对比损失（如CLIP相似度损失）优化该伪词嵌入，使其在视觉-语言嵌入空间中更贴近遥感图像中的对应视觉特征。
2. **推理流程**：  
   - 仅将优化后的文本嵌入（可组合为“a photo of [V]”模板）输入冻结的分割模型。
   - 模型计算像素与文本嵌入的相似度，输出分割掩码。
3. **优势**：不依赖视觉提示，保持开放词汇的灵活性；无需重新训练模型。

## 3. 实验设计
- **基准数据集**：以**遥感开放词汇分割代表性基准**为主，评估受影响类别（如“游泳池”、“裸土”等）的性能。
- **额外数据集**：在**8个遥感数据集**上（如Potsdam, Vaihingen, LoveDA, ISPRS等）进行跨场景验证。
- **对比方法**：
  - **基线**：原始CLIP文本查询（标准类别名称）、自然语言改述（手工改写类别名称）。
  - **少样本方法**：注入视觉提示的方法（如ViP-Adapter、MaskCLIP+等），这些方法在推理时需要参考图像或掩码。
- **指标**：平均交并比（mIoU），尤其关注受影响类别（低基线的类别）的mIoU。

## 4. 资源与算力
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等算力细节。只提及模型是冻结的，仅学习少量文本嵌入，计算开销应较低。

## 5. 实验数量与充分性
- **实验组数**：包括主要基准实验、8个遥感数据集的对比、消融实验（如不同示例数量、不同初始化方式等）。
- **充分性评估**：
  - 实验设计相对充分：覆盖了多个数据集、多种基线方法（自然语言改述、视觉提示注入方法）。
  - 消融实验验证了文本反转的必要性：改述只能部分改善，而文本反转能大幅提升。
  - 对比方法包括其他少样本方法，确保了公平性（推理时仅文本 vs. 需视觉提示）。
- 但未提供与全监督方法的对比，也未讨论模型在其他非遥感场景上的退化风险。

## 6. 主要结论与发现
- 遥感开放词汇分割中性能不佳的根源在于类别名称在嵌入空间中定位弱，而非模型本身。
- **更好的名称**（自然语言改述）能部分弥补差距（例如mIoU提升数个点）。
- **文本反转**可从少量示例中恢复弱地址，大幅提升受影响类别的mIoU（从3.9提升至39.4）。
- 该方法在8个遥感数据集上优于那些在推理时注入视觉提示的少样本方法。
- 推理时仅依赖文本，保持了开放词汇的简便性与泛化能力。

## 7. 优点
- **方法简洁有效**：仅学习少量文本嵌入，无需修改模型参数，易于实现和部署。
- **推理高效**：推理时无需额外视觉提示，计算成本低，适合遥感大范围应用。
- **定位问题根源**：通过实验证明文本查询是关键瓶颈，而非模型架构，提供了清晰的研究思路。
- **跨数据集验证**：8个数据集上的结果增强了结论的鲁棒性。

## 8. 不足与局限
- **算力信息缺失**：未报告训练文本嵌入所需的资源，不利于复现和比较。
- **依赖少量示例**：尽管是少样本，但仍需每个类别提供带掩码的图像，实际遥感标注成本高。
- **未比较全监督或更强基线**：未与基于微调的全监督分割方法对比，无法评估上限。
- **潜在偏差风险**：实验仅聚焦于“受影响类别”，未分析对其他类别性能的影响（可能轻微下降）。
- **应用限制**：对于新类别，仍需获取少量示例并重新学习嵌入，无法做到零样本。
- **未讨论类别数量扩展性**：当类别数量很多时，同时学习多个伪词是否互相干扰尚不清楚。

（完）
